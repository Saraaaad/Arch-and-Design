package org.example.tourism.common;

public enum Role {
    ADMIN,          // Can do everything
    HOTEL_MANAGER,  // Can manage hotels and room types
    GUEST           // Can only search and book
}